在了解了JVM的栈和堆之后，探讨垃圾回收（Garbage Collection, 简称GC）是极其顺理成章的。因为**堆是存放对象的大本营，自然也就是垃圾回收的主战场**；而**栈，则是寻找垃圾回收线索的起点**。

在C/C++中，程序员需要手动分配和释放内存（`malloc`/`free` 或 `new`/`delete`），稍有不慎就会导致内存泄漏。而Java最吸引人的特性之一，就是把这个脏活累活交给了JVM的垃圾收集器（GC）来自动完成。

要想搞懂GC，我们需要解决两个核心问题：**怎么找出垃圾？** 以及 **怎么清理垃圾？**

---

### 1. 怎么找出垃圾？（可达性分析算法**在程序运行的当前时刻，肯定还活着的、绝对不能被回收的对象**。

主要包括以下几类（结合我们之前讲的内存区域来看会非常清晰）：

- **① 虚拟机栈（栈帧中的局部变量）引用的对象：**
    
    - _例子：_ 你在方法里写了一句 `A a = new A();`。只要这个方法还没执行完，栈帧里的局部变量 `a` 就还在，它指向的堆里的那个 `new A()` 对象就绝对不能被回收。所以，当前所有活动线程的栈帧里的局部变量，都是 GC Root。
        
- **② 方法区中类静态属性引用的对象：**
    
    - _例子：_ `public static Object myStaticObj = new Object();`。静态变量属于类级别，只要类还在，它引用的对象就必须活着。
        
- **③ 方法区中常量引用的对象：**
    
    - _例子：_ `public static final String MY_CONSTANT = "hello";`。常量引用的对象同样作为根节点。
        
- **④ 本地方法栈中 JNI（即一般说的 Native 方法）引用的对象：**
    
    - Java调用C/C++底层代码时引用的对象。
        
- **⑤ JVM 内部的引用：**
    
    - 比如基本数据类型对应的 Class 对象，一些常驻的异常对象（如 `NullPointerException`），以及系统类加载器等。
        
- **⑥ 所有被同步锁（`synchronized` 关键字）持有的对象。**
    

**总结一下 GC Root 的本质：** 它们通常存放在**栈**（局部变量、Native方法）或者**方法区**（静态变量、常量）中，而不是堆里。GC的过程，本质上就是从栈和方法区出发，去堆里面“查户口”的过程。

---

### 3. 怎么清理垃圾？（常见的垃圾回收算法）

找出了垃圾，下一步就是清理。JVM 有几种基础的清理策略：

- **标记-清除（Mark-Sweep）：**
    
    - 最基础的算法。顺藤摸瓜把活着的“标记”出来，然后把没标记的直接“清除”。
        
    - _缺点：_ 会产生大量的内存碎片，就像一排座位中间空出了很多零散的位子，大对象进来坐不下。
        
- **标记-复制（Mark-Copy）：**
    
    - 我们在上一个问题提到的新生代（Survivor区）用的就是这个。把内存分两半，每次只用一半。清理时，把活着的对象全部“复制”到另一半去，按顺序挤一挤，然后把原来那一半直接清空。
        
    - _优点：_ 没碎片，速度快。
        
    - _缺点：_ 浪费了一半的内存空间（所以新生代的 Eden 和 Survivor 比例才调成了 8:1:1，而不是 1:1）。
        
- **标记-整理（Mark-Compact）：**
    
    - 这是**老年代**常用的算法。先把活对象标记出来，然后让所有存活的对象都向内存空间的一端移动（就像上课时让所有人往前排坐），最后直接清理掉边界以外的内存。
        
    - _优点：_ 既没有碎片，也不浪费空间。
        
    - _缺点：_ 移动对象需要更新所有引用它的地方，比较耗时。
        

为了让你更直观地看到 GC Roots 是如何通过引用链“保住”对象，以及那些“断了线”的对象和“循环引用”的对象是如何被回收的，你可以通过下面的沙盒模拟一次可达性分析的过程：