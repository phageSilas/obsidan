1) 大模型  （Large Language Model，LLM）
2) **Temperature(温度)**
3) **Top-P（核采样）**
4) MoE(混合专家架构)
5) 思维链(Chain of Thought)



6) max_tokens(最多生成多少token)
7) stream
8) SSE(Server-Sent Events, 服务端推送事件）协议

9) **Prompt** 
10) 示例驱动（Few-shot Learning）

11) **RAG** **（Retrieval-Augmented Generation，检索增强生成）**
12) **幻觉** （Hallucination）:生成看起来很合理、但实际上完全错误的内容。

13) Apache Tika:是 Apache 基金会的开源项目，专门用于**内容检测和内容提取**。
14)  MIME（Multipurpose Internet Mail Extensions）
15) 

16) **分块（Chunking）** 
17) **chunkSize（块大小）和 overlap（重叠量）** 
18) 固定大小分块（Fixed Size Chunking）
19) 重叠分块（Overlapping Chunking）
20)  递归分块（Recursive Chunking）
21)  语义分块（Semantic Chunking）
22)  混合分块（Hybrid Chunking）

23) 元数据（Metadata）元数据的本质：给每个 chunk 贴标签
24) ACL(Access Control List, 访问控制列表)  权限控制
25) 


26)  向量（Vector）
27) **向量化（Embedding）** 
28) 语义相近 = 向量相近


29) 向量数据库
30) **ANN**（Approximate Nearest Neighbor，近似最近邻搜索）
31) IVF（倒排文件索引）
32) HNSW(Hierarchical Navigable Small World Graph,分层可导航小世界图）

33) 混合检索（Hybrid Search）[向量检索策略与召回优化](https://articles.zsxq.com/id_d0s0gr835j0n.html)
34) BM25（Best Matching 25）算法
35) 逆文档频率(Inverse Document Frequency,IDF) 出现得越少，IDF 越高
36) RRF（倒数排名融合）
37) **重排序（Reranking）** 
38) 快召回(向量检索 / 混合检索） + 慢精排Cross-Encoder（交叉编码器）

39) **Function Call（函数调用）** 

[MCP](https://articles.zsxq.com/id_6f38qekvjsmf.html)
1) **MCP（Model Context Protocol, 模型上下文协议）协议** 
2)  Stdio（标准输入输出）
3) Streamable HTTP（可流式 HTTP）
4)  Host（宿主应用）
5) Resources（资源）[Resources](https://articles.zsxq.com/id_frrdwwexjhng.html)
6) Prompts（提示词模板）